name: Test

on:
  pull_request:
  push:
    branches:
      - main
      - dev
    paths-ignore:
      - "**/*.md"
      - "LICENSE*"
      - ".github/workflows/precompile.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux:
    runs-on: ubuntu-latest
    env:
      MIX_ENV: test
    steps:
      - uses: actions/checkout@v5
      - uses: erlef/setup-beam@v1
        with:
          otp-version: 25.3
          elixir-version: 1.14

      - name: Install dependencies
        run: mix deps.get

      - name: Compile
        run: mix compile

      - name: Test
        run: mix test --no-compile

  windows:
    runs-on: windows-2022
    env:
      MIX_ENV: test
    steps:
      - uses: actions/checkout@v5

      - uses: erlef/setup-beam@v1
        with:
          otp-version: 25
          elixir-version: 1.14

      - uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install dependencies
        shell: bash
        run: mix deps.get

      - name: Compile
        shell: bash
        run: mix compile

      - name: Test
        shell: bash
        run: mix test --no-compile

  macos:
    runs-on: macos-14
    env:
      MIX_ENV: test
      OTP_VERSION: "26.2.1"
      ELIXIR_VERSION: "1.16.0"

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install OTP and Elixir
        run: |
          curl -fsSO https://elixir-lang.org/install.sh
          sh install.sh "elixir@${ELIXIR_VERSION}" "otp@${OTP_VERSION}"
          export OTP_MAIN_VER="${OTP_VERSION%%.*}"
          echo "$HOME/.elixir-install/installs/otp/${OTP_VERSION}/bin" >> "$GITHUB_PATH"
          echo "$HOME/.elixir-install/installs/elixir/${ELIXIR_VERSION}-otp-${OTP_MAIN_VER}/bin" >> "$GITHUB_PATH"

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Compile
        run: mix compile

      - name: Test
        run: mix test --no-compile
